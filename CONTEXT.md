# JoyDSH 领域语境

JoyDSH 是一个手柄优先的智能体工作空间，用于在显著减少鼠标依赖的情况下完成开发任务。

## 统一语言

**JoyDSH**：
面向用户的智能体工作空间，以手柄优先的体验组织项目、任务会话、审批和工作成果。
_避免使用_：桌面外壳、启动器、集成开发环境

**工作空间**：
用户选中的项目以及与该项目关联的任务上下文。
_避免使用_：桌面、首页、仓库浏览器

**任务会话**：
围绕一个用户目标形成的持久记录，包含智能体执行的工作以及产生的决定和成果。
_避免使用_：聊天、线程、对话

**任务闭环**：
用户从提出目标开始，经过必要的等待处理和执行观察，最终检查、保留或撤销工作成果的完整过程。
_避免使用_：功能对齐、界面复刻

**手柄优先**：
一种交互约定：所有高频非文本操作都能通过手柄以可达、可预测且高效的方式完成，长文本仍可使用键盘或语音输入。
_避免使用_：仅限手柄、鼠标模拟

**外部听写桥**：
JoyDSH 将手柄或键盘产生的语音输入动作转交给外部听写工具，并让转写结果进入当前有效文本输入目标的交互边界。
_避免使用_：Spokenly 专用集成、内置语音识别

**智能体运行时**：
负责运行任务会话并向 JoyDSH 提供智能体能力的可替换执行环境。
_避免使用_：界面后端、JoyDSH 核心

**审批**：
用户对任务会话中敏感操作作出的明确允许或拒绝决定；确认成本随操作风险提高。
_避免使用_：确认框、权限弹窗

**待回应**：
任务会话继续推进前需要用户处理的信息请求，统一聚合审批、问题和方案审阅，但不改变三者各自的语义。
_避免使用_：待审批、统一审批

**纠偏输入**：
用户明确要求立即影响当前执行回合的输入，与等待当前回合结束后处理的排队输入相区分。
_避免使用_：插队消息、紧急消息

**执行清单**：
智能体在任务会话中维护的实施步骤及其进度，用于观察正在进行的工作。
_避免使用_：计划、方案

**方案模式**：
智能体在实施前探索并形成可审阅方案的任务会话模式，获批方案通过方案审阅退出该模式。
_避免使用_：计划模式、只读模式

**持续目标**：
任务会话中可跨多个执行回合自动推进，并具有轮次、暂停、阻塞和完成状态的长期目标。
_避免使用_：用户目标、后台任务、自动化

**子任务**：
智能体为完成父任务而委派的从属任务会话，其状态和结果归属于父任务。
_避免使用_：后台任务、独立任务、子聊天

**派生任务**：
用户从任务会话的稳定历史边界创建的独立任务会话，用于尝试替代方案并保留来源关系。
_避免使用_：子任务、复制任务

**后台作业**：
任务会话内由工具启动并在后台持续运行的工作，具有可读取的状态和输出，但不是独立任务会话。
_避免使用_：后台任务、子任务、终端窗口

**智能体预设**：
创建任务会话时选择的智能体能力组合，决定该任务可使用的工具和执行方式。
_避免使用_：模型、角色、插件包

**前台任务**：
当前占据主要工作视图并直接接收用户输入的唯一任务会话。
_避免使用_：活跃聊天、选中线程

**后台任务**：
不占用主要工作视图，但仍可观察其运行或暂停状态的任务会话。
_避免使用_：隐藏任务、最小化窗口

**命令中心**：
无需离开前台任务即可切换任务、处理待回应、查看通知和调用常用命令的临时控制界面。
_避免使用_：控制中心、快捷菜单、工具栏

**完全访问**：
一种明确的权限模式：智能体继承当前系统用户的文件系统、网络和命令执行权限，不再逐项请求审批。
_避免使用_：绕过、部分绕过、无限制审批

**标准权限**：
一种以当前工作空间为默认边界，并依据操作风险决定是否需要审批的权限模式。
_避免使用_：受限模式、安全模式
