# 项目协作约定

## 沟通与执行边界

- 始终使用中文回复。
- 先阅读仓库内的代码、文档、配置和既有约定；能够从仓库确认的事实不得反问用户。
- 只修改完成当前任务所必需的文件，不覆盖或回退用户已有改动。
- 用户只要求分析、审查、解释或方案时，不得修改文件；用户明确要求实现、修复或优化时，才执行变更。

## 项目命令与工具链

- 优先使用仓库已经声明的运行时、包管理器、锁文件和脚本，不自行引入第二套工具链。
- 依赖变更必须同步更新对应锁文件，禁止无关的全量依赖升级或锁文件重写。
- 自动生成文件应通过其生成源或生成命令更新；标有“不要手动编辑”的文件不得直接修改，除非仓库中不存在生成源且用户明确授权。

## 前端设计风格
查看 DESIGN.md

## 文件大小和函数粒度

以下规则中，“必须”是硬性要求，“优先”是默认倾向；确有例外时应在交付说明中给出理由。

- 新增源文件优先控制在 250 行以内。
- 超过 300 行的文件继续新增逻辑前，必须先评估并优先拆分职责。
- 单个函数优先控制在 40 行以内。
- 超过 60 行的函数必须优先拆成命名清晰的辅助函数。
- 渲染函数、请求处理函数、worker 主流程函数不得混合数据读取、校验、状态变更、HTML 拼接和副作用；至少按职责拆开。
- 大段模板字符串、长条件分支、重复 HTML 片段、重复 fetch/response 逻辑应抽成小函数。
- 上述行数不包含纯数据、类型声明、生成文件和测试夹具；但这些文件仍需保持单一职责和可维护性。

## 变更与验证

- 实现前先确认影响范围和现有测试；修复缺陷时优先补充能够复现问题的测试。
- 完成改动后，根据项目已有能力执行格式检查、lint、类型检查、相关测试和生产构建。
- 不得声称未实际执行的检查已经通过。因环境、权限或外部依赖无法验证时，必须说明未验证内容、原因和风险。

## 环境变量与敏感信息

- 真实 `.env`、密钥、证书和生产配置不得提交到仓库。
- 新增环境变量时，应同步更新示例配置或环境变量文档，说明用途、是否必填、默认行为和暴露范围。
- 不得在日志、截图、提交、PR 描述或回复中暴露敏感信息。

## Git 与提交

- 禁止直接在 `main` 上开发或推送；所有改动通过功能分支和 PR 合入。
- 提交信息使用语义化格式，摘要使用中文。
- 未经用户明确要求，不得提交、推送、强制推送、创建 PR 或合并 PR。

## PR 提交流程

当用户说“提 PR”时，按以下流程执行：

1. 确认当前不在 `main`，工作区变更已按任务范围整理并提交。
2. 执行 `git fetch -p` 更新远端引用并清理已删除的引用。
3. 执行 `git rebase origin/main`；如有冲突，理解双方意图后解决并完成 rebase，不得丢弃用户改动。
4. 完成必要验证后推送当前分支；rebase 改写了已推送提交时使用 `git push --force-with-lease`。
5. 当前分支已有 PR 时更新，否则创建；标题和描述需综合相对 `origin/main` 的全部提交与实际 diff。

## 部署约定

- 合并或推送到 `main` 会自动触发线上部署，因此不得将 `main` 用作试验分支。
- 未经用户明确授权，不得手动执行生产部署、配置变更、迁移或回滚。

## Feature 文档约定

- 仓库根目录必须维护 `CHANGELOG.md`；实现每个面向用户的 feature 时，至少在 `Unreleased` 下新增一条简短、面向结果的变更记录。
- `CHANGELOG.md` 按 feature 更新，不要求每个提交单独记录；同一 feature 的多个提交应合并为一条或一组紧凑记录。
- 纯内部重构、测试调整、格式化和不改变用户结果的配置维护，可不单独增加变更记录。
- feature 完成前必须检查本次改动是否引入或改变领域术语、实体边界、生命周期、状态转换、业务不变量或职责归属。
- 存在领域变化或术语含义得到进一步明确时，必须创建或更新对应的 `CONTEXT.md`；纯技术变更无需修改。

## 决策澄清与 GRILL 工作流

首次准备 Codex 环境且确认缺少相关技能时，只执行一次：

```bash
npx skills@latest add mattpocock/skills --skill grill-me --skill grill-with-docs --skill grilling --skill domain-modeling -a codex -g -y
```

- 对目标清晰、影响局部、可逆、已有项目惯例可循的问题，直接实现，不提澄清问题，不启动 GRILL。
- 只有关键决策会显著改变产品行为、领域边界、架构、数据模型、外部契约、迁移方案或安全风险，且无法从现有上下文可靠判断时，才启动 GRILL。
- 用户明确要求讨论、挑战方案或进行 GRILL 时，也可以直接启动。
- 更新 `CONTEXT.md` 不以启动 GRILL 为前提；发现领域语言需要沉淀时，可以直接使用 `domain-modeling`。

启动 GRILL 后按以下方式编排：

1. 普通方案或计划决策使用 `grill-me`，将其视为一次完整的 `grilling` 会话，不再单独重复调用 `grilling`。
2. 涉及领域术语、实体边界、生命周期、不变量，或需要同步沉淀 ADR、术语表时，直接使用 `grill-with-docs`；不要再分别调用其他三个技能。
3. 一次只围绕真正阻塞决策的最小问题集追问，优先给出选项、影响和推荐；已经回答或能推断的问题不得重复询问。
4. 获得足够信息后立即结束访谈，简要记录决定、理由和未决风险，然后继续实施。



# JoyDSH 智能体说明

## Agent skills

### 问题跟踪器

问题和规格以本地 Markdown 文件保存在 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### 分诊标签

使用默认的五类分诊标签。详见 `docs/agents/triage-labels.md`。

### 领域文档

采用单语境布局：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。

