# JoyDSH 前端设计约定

JoyDSH 是面向电视距离与桌面距离的手柄优先工作界面。设计目标是让任务状态、待回应和成果检查保持清晰、稳定、可扫描，不采用营销页式布局。

## 视觉基调

- 使用深色中性背景、青色主操作、绿色成功、黄色等待、红色危险的多色语义体系。
- 页面主体保持连续工作面，不把每个区域包装成悬浮卡片，也不嵌套卡片。
- 控件圆角默认不超过 `6px`；圆形只用于状态点、头像或明确的圆形图标按钮。
- 字体使用系统无衬线；标识符、会话 ID、路径和代码使用等宽字体。
- 字距固定为 `0`，不按视口宽度缩放正文或控件字号。

## 布局

- 顶栏、任务会话栏、主任务区和任务检查器使用稳定网格；动态内容不得改变一级布局尺寸。
- 主任务区是视觉重心。任务检查器隐藏后，主任务区占满剩余空间。
- 弹窗、命令中心和选择层只用于临时决策，不承载常驻页面结构。
- 桌面端优先保证远距离可读性；窄屏允许纵向重排，但不得出现横向页面滚动或文字遮挡。
- 固定格式元素使用明确的最小尺寸、网格轨道或 `aspect-ratio`，避免加载和状态变化引发布局跳动。

## 控件

- 熟悉的工具操作使用 Lucide 图标按钮，并提供 `title` 与 `aria-label`。
- 清晰命令使用图标加文字按钮；二元设置使用开关或复选框，模式使用分段控件，选项集使用菜单或选择器。
- 主要、次要、危险操作必须有不同的颜色和层级，危险操作不得依赖位置区分。
- 按钮、输入框和选择器默认高度适合手柄焦点与电视距离操作；紧凑区域也不得压缩可点击范围。
- 文字必须在容器内换行或截断，不能与图标、徽标和相邻控件重叠。

## 焦点与输入

- 所有高频非文本操作必须进入 managed focus graph，并同时支持键盘和手柄。
- 焦点态使用高对比青色边框、外轮廓和轻微缩放；不得只靠颜色或细微阴影表达。
- 手柄模式隐藏鼠标残留影响；鼠标重新移动或点击后恢复指针语义。
- 文本输入聚焦时保留光标编辑行为；全局命令、截图和外部听写桥等明确快捷操作除外。
- 弹层打开时焦点进入弹层，关闭后恢复到触发控件；后台待回应不得强制抢占前台焦点。

## 状态与内容

- 使用项目领域术语：任务会话、待回应、方案模式、执行清单、前台任务和后台任务。
- 运行、等待、成功、失败和不可用状态必须同时提供文字或图标，不只显示颜色。
- 错误信息说明发生了什么以及用户可执行的下一步，不把请求已发送表述为外部系统已成功完成。
- 任务流优先展示真实内容和结果；工具内部指令、系统注入和重复状态信息应折叠或隐藏。
- 空状态只提供当前可执行动作，不使用大段功能说明或营销文案。

## 变更检查

前端变更至少检查：

- 键盘、鼠标和手柄三种输入路径。
- 焦点进入、移动、弹层关闭与恢复。
- 桌面宽屏和窄屏下的溢出、遮挡与布局稳定性。
- 加载、空、成功、失败、禁用和等待回应状态。
- 图标可访问名称、文本对比度和危险操作层级。
