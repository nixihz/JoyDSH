# 同时提供标准权限与完全访问

JoyDSH 同时提供“标准权限”和“完全访问”两个明确的权限预设。标准权限以工作空间隔离和风险审批为默认安全边界；完全访问等同 Codex 的全部权限模式，智能体继承当前系统用户的文件系统、网络和命令执行权限，不再逐项审批。两种模式必须在任务界面持续可见，不能把完全访问实现为标准权限中含义模糊的临时绕过。

## 后续影响

完全访问不会承诺额外拦截破坏性操作，但固定的立即暂停能力和完整审计记录仍然有效。权限模式可以在创建任务时选择，并可保存为工作空间默认值。
