# 使用语义动作和受控焦点

JoyDSH 将手柄输入建模为“确认”“返回”“下一个区域”和“打开命令中心”等语义动作，并为每个交互界面维护受控的焦点关系。产品行为不依赖特定手柄的按键名称，也不使用指针模拟，使 DualSense、Xbox 兼容手柄、键盘和鼠标共享同一套交互约定。

## 后续影响

每个界面都必须定义确定性的焦点进入、移动、恢复和退出行为。平台特定的按键图标和物理按键映射由输入适配器负责，自动化测试则独立于具体手柄硬件验证语义导航。
