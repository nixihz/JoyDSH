# 工作空间权限默认值

Type: task
Status: resolved
Blocked by: 02

## Scope

持久化每个项目的权限预设，并在项目中心选择后应用到任务。

## Comments

## Answer

工作区目录已按项目持久化 `standard` / `full-access` 默认值，旧目录配置默认迁移为标准权限。创建、打开和恢复项目时会把所选权限下发给对应 DSH 会话。
