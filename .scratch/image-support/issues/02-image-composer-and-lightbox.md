# 输入组件支持图片上传与待发送轨道

Type: task
Status: resolved

## Scope

- 实现 `AttachmentRail` 组件（缩略图、删除、点击原图预览）。
- 支持剪贴板粘贴（`onPaste`）提取图片。
- 支持拖拽上传（Drag & Drop）与拖拽遮罩指示。
- 支持原生文件选择器。
- 实现 `ImageLightbox` 全屏模态灯箱组件。
- 焦点图与快捷键支持。

## Comments

实现规格见 `../spec.md`。
