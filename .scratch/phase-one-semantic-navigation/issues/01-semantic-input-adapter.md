# 01 语义输入适配器

Type: task
Status: resolved

## 目标

建立可测试的键盘和标准 Gamepad 语义动作映射，并提供浏览器轮询适配器。

## 验收

- 方向、确认、返回、主要操作、更多操作、区域切换、内容页切换和命令中心具有稳定动作名。
- 长按方向输入具备受控重复，普通按钮只在按下边沿触发。
- 键盘映射不会在文字输入模式中接管方向键。

## Answer

已建立 `@joydsh/input`。键盘与标准 Gamepad 共用语义动作；普通按键按下边沿触发，方向输入在初始延迟后受控重复，断连时映射器会重置。
